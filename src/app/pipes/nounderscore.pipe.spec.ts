import { NounderscorePipe } from './nounderscore.pipe';

describe('NounderscorePipe', () => {
  it('create an instance', () => {
    const pipe = new NounderscorePipe();
    expect(pipe).toBeTruthy();
  });
});
